services:
  sentinel-flow:
    build: .
    environment:
      PORT: 4000
      NODE_ENV: production
    ports:
      - "4000:4000"
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
